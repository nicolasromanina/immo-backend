export enum Role {
  ADMIN = 'admin',
  PROMOTEUR = 'promoteur',
  MANAGER = 'manager',
  USER = 'user',
  GUEST = 'guest',
  AUDITOR = 'auditor',
  SUPPORT = 'support',
  // Ajoutez d'autres rôles ici si besoin
}
